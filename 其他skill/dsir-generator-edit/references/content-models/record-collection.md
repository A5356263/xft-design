# `record_collection（记录集合）`

## 定义

用于表达一组可扫描、可定位、可处理的记录集合。

## 最小结构

- `columns（记录字段列）`
- `records（记录项）`

## 可选信息项

- `row_actions（行级动作）`
- `batch_context（批量上下文）`
- `summary_fields（摘要字段）`

## 常见状态

- `default（默认）`
- `empty（空）`

## 常见使用方

- `record_table（记录表格块）`
