# 第 5 步素材覆盖说明

本步以 `material-inventory-full.csv` 的 91 张素材为总基准。

- 已转成本步模块资产的素材，状态为 `converted_in_step5`。
- 页面配方类素材，状态为 `covered_in_step3_page_recipe`。
- 区域/导航类素材，状态为 `covered_or_deferred_region_rule`。
- 反馈、状态、覆盖层，状态为 `deferred_step6_feedback_state_overlay`。
- 组件组合、按钮规则，状态为 `deferred_step6_or_step7_combo_rule`。

后续步骤必须继续沿用 `material_id`，避免遗漏素材。
